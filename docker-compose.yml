services:
  web:
    image: tissuepro-fe
    container_name: tissuepro-fe
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api.tissuepro.id}
    ports:
      - "5175:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
